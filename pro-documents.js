/* ============================================================
   DOCUMENTOS — DocuStream PRO · datos.gob.es
   ============================================================ */

// Esta función es llamada desde demo.html cuando llegan datasets reales.
// renderDocList() y selectDataset() ya están definidas en demo.html.
// Este archivo queda como punto de entrada por compatibilidad.

// Si la página carga sin búsqueda previa, documentsList ya tiene
// el estado inicial puesto por demo.html (spinner → resultados).
// No hacemos nada extra aquí para no interferir.
